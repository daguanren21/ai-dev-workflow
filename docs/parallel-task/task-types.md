# 任务类型与调度策略

> 定义任务分类、并行策略、声明语法与技能匹配机制

---

## 任务类型

| 类型 | 标识 | 并行策略 | 默认 Review | 说明 |
|-----|------|---------|------------|------|
| 代码开发 | `code:dev` | `isolated` | strict | 同模块串行，跨模块并行 |
| 代码修复 | `code:fix` | `isolated` | standard | 同文件串行 |
| 代码重构 | `code:refactor` | `serial` | strict | 全局串行，影响范围大 |
| 文档编写 | `doc:write` | `parallel` | light | 完全并行 |
| 文档翻译 | `doc:translate` | `parallel` | light | 完全并行 |
| 调研分析 | `research` | `parallel` | light | 完全并行，支持缓存 |
| 数据处理 | `data` | `isolated` | standard | 按数据源隔离 |
| 测试执行 | `test` | `parallel` | standard | 完全并行 |

## 并行策略

```
parallel  → 完全并行，无限制
isolated  → 按隔离键分组，组内串行，组间并行
serial    → 强制串行执行
```

### parallel（完全并行）

适用于无共享状态的独立任务，如文档编写、调研、测试。多个任务可同时执行。

### isolated（隔离并行）

按隔离键（文件路径/模块名/数据源）分组：
- 同一隔离键内串行执行（避免冲突）
- 不同隔离键间并行执行

示例：`@isolated(auth/)` 表示按 `auth/` 模块隔离。

### serial（强制串行）

全局只允许一个任务执行，适用于大范围重构等影响面广的操作。

## 智能调度算法

```
1. 解析任务列表，识别任务类型
2. 按类型分配并行策略
3. 对 isolated 类型提取隔离键（文件路径/模块名/数据源）
4. 生成执行计划：
   - 并行组：可同时执行的任务集合
   - 串行链：必须顺序执行的任务序列
5. 控制最大并行度：≤5 个并行任务
```

## 任务声明语法

```markdown
## TaskGroup: <任务组名称>

### Meta
- parallel_limit: 5
- review_level: standard
- on_failure: continue | stop

### Tasks
1. [code:dev] 实现用户认证模块 @isolated(auth/)
2. [code:dev] 实现订单模块 @isolated(order/)
3. [doc:write] 编写 API 文档
4. [research] 调研支付网关方案 @cache(7d)
```

### 语法说明

| 元素 | 格式 | 说明 |
|------|------|------|
| 任务类型 | `[type:subtype]` | 方括号中的任务类型标识 |
| 隔离键 | `@isolated(key)` | 指定隔离分组的键 |
| 缓存 | `@cache(duration)` | 调研任务缓存有效期 |
| 依赖 | `@depends(task_id)` | 指定前置依赖任务 |

### Meta 参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|-------|------|
| `parallel_limit` | number | 5 | 最大并行任务数 |
| `review_level` | string | standard | light / standard / strict |
| `on_failure` | string | continue | continue（跳过失败继续）/ stop（停止所有） |

## 技能匹配机制

### 五级查找

| 层级 | 查找源 | 内容 | 特点 |
|:---:|-------|------|------|
| L1 | 项目 `skills/` | 项目专属规范 | 最高优先级 |
| L2 | 全局 skills | 通用开发规范 | superpowers 等 |
| L3 | skills.sh 生态 | 社区 skills | find-skills |
| L4 | Context7 MCP | 框架/库官方文档 | 结构化、可信 |
| L5 | WebSearch | 网络最佳实践 | 兜底，需验证 |

### 匹配输出格式

```markdown
## 技能匹配结果

### Level 1: 项目内 Skills
- ✅ `component-lib` - UI组件规范 (匹配: Table 组件)

### Level 2: 全局 Skills
- ✅ `superpowers:test-driven-development` - TDD 规范

### Level 3: skills.sh 生态
- 📦 `vercel-labs/frontend-design` - 前端设计规范

### Level 4: Context7 文档查询
- 📚 `Vue3 Composition API` - 官方文档

### Level 5: WebSearch 补充
- 🔍 "Vue3 最佳实践" → [来源链接]
```
